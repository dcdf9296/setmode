import { MoreVerticalIcon } from "lucide-react"

interface MoreVerticalProps {
  className?: string
}

export default function MoreVertical({ className }: MoreVerticalProps) {
  return <MoreVerticalIcon className={className} />
}
